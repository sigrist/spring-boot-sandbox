
dependencies:
  post:
    - ./gradlew

# Artefatos que devem ser salvos
general:
  artifacts:
    - "build/libs" # relative to the build directory
    - "build/distributions" # relative to the user's home directory
