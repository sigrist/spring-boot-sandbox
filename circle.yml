
dependencies:
  post:
    - ./gradlew

# Artefatos que devem ser salvos
general:
  artifacts:
    - "build/libs/*.jar" # relative to the build directory
    - "build/distributions/*.deb" # relative to the user's home directory
